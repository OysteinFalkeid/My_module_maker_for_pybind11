def add(a: float, b: float) -> float:
    """add(a: float, b: float) -> float

    A function that adds two numbers 

    Args:
       a (float): first number
       b (float): scound number

    Examples:
       >>> add(2.0, 3.0)
       5.0
    """
def fib(n: int) -> int:
    """fib(n: int) -> int

    returns fibonachi number n for n innput
    """
